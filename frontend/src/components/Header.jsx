// Simple top bar for the app. Its own component just so App.jsx
// doesn't get cluttered.

function Header() {
  return (
    <header className="app-header">
      <h1>SmartDoc</h1>
    </header>
  );
}

export default Header;
